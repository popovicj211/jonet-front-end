import axios from 'axios';
/*
const Services = async (setServices => {

    axios.get(url, null, {
        Accept : 'application/json'
    }).then(response => {
      setService(response.data)
    }).catch(error => {
      const showErrorMessage = StatusMessages(error, lang, ErrorMessages())
      console.error(showErrorMessage)
    });
    
   
})*/

/*
const Services = (setServices ) => {
 // let url = `${process.env.REACT_APP_API_URL}/services`;
 let url = `${process.env.PUBLIC_URL}/data/services.json`;
   axios.get(url, null, {
      Accept : 'application/json'
  }).then(response => {
    setServices(response.data)
  }).catch(error => {
   // const showErrorMessage = StatusMessages(error,  ErrorMessages())
    console.error(error);
  });

}

const Service = (setService , service) => {
  let url = `${process.env.PUBLIC_URL}/data/service.json`;

   axios.get(url, null, {
      Accept : 'application/json'
  }).then(response => {
    setService(response.data)
  }).catch(error => {
   // const showErrorMessage = StatusMessages(error,  ErrorMessages())
    console.error(error);
  });

}

export{
  Services,
  Service
}

*/